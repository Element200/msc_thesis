\documentclass{standalone}

\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{siunitx}
\usepgfplotslibrary{statistics}
\pgfplotsset{compat=1.18}
\newcommand{\kB}{k_{\mathrm{B}}}
\DeclareSIUnit{\pixel}{px}

\begin{document}
\begin{tikzpicture}
    \begin{axis}%
        % [xlabel = $x/x_s$, ylabel = $\frac{U(x/x_s)}{k_{\mathrm B}T_c}$] % inferred_quadratic
        % [xlabel = $x/x_s$, ylabel=$p(x/x_s)$] % brownian_PDF
        [xlabel=\Large{$t$ (\unit{\second})}, ylabel=\Large{$x/x_s$}, width=\textwidth, height=0.01\textheight] % rawdata_downsampled
        % [ylabel=\Large{$S(f)$ ($x_s^2/$\unit{\hertz})}, xlabel = \Large{$f$ (\unit{\hertz})}] % PSD

    \addplot[blue, thin] table[col sep=comma, x=t, y=x]{x1_downsampled.csv}; % rawdata_downsampled

    % % \addplot+[fill=blue!40, hist={density, data=y, bins=50}, draw=none, no marks] table[col sep=comma, x=t,y=x] {data_downsampled.csv};
    % \addplot+[ybar,bar width=0.09905544, no marks, fill=blue!40] table[x=x,y=p, col sep=comma] {gaussian_data.csv};
    % % \addlegendentry{Empirical PDF}
    % \addplot[red, thick, domain=-1:1, samples=1000]{exp(-0.5*14.9560588*(x-0.02204114)^2+0.5*ln(14.9560588/(2*pi)))}; % See ipython file for curve fit code. 
    % % \addlegendentry{Fit to $\exp(-k(x-x_0)^2/(2\kB T_c)+\ln(k/(2\pi\kB T_c)))$}

    % brownian_PDF

    % \addplot[only marks, red] table[col sep=comma, x=x,y=E]{gaussian_data.csv};
    % \addplot+[only marks,error bars/.cd,y dir=both,y explicit] table[x=x,y=E,y error=dE,col sep=comma]{gaussian_data.csv};
    % \addplot[red, thick, domain=-1.5:1.5, samples=200] {0.5*14.9560588*(x-0.02204114)^2-0.5*ln(14.9560588/(2*pi))}; % See ipython for curve fit

    % \addplot[blue!50, thin, no marks] table[col sep=comma, x=f, y=S]{psd.csv};
    % \addplot[olive, thick, no marks] table[col sep=comma, x=f, y=S_pred]{psd.csv};
    \end{axis}
\end{tikzpicture}
\end{document}