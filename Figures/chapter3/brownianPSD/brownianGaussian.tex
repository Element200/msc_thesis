\documentclass{standalone}

\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{siunitx}
\usepgfplotslibrary{statistics}
\pgfplotsset{compat=1.18}
\newcommand{\kB}{k_{\mathrm{B}}}
\DeclareSIUnit{\pixel}{px}

\begin{document}
\begin{tikzpicture}
    \begin{loglogaxis}%
        % [xlabel = $x$ (\unit{\pixel}), ylabel = $U(x)/k_BT_c$] % inferred_quadratic
        % [xlabel = $x$ (\unit{\pixel}), ylabel=$p(x)$ ($\unit{\per\pixel}$)] % brownian_PDF
        % [xlabel=$t$ (\unit{\second}), ylabel=$p(x)$ ($\unit{\pixel}$), width=\textwidth, height=0.01\textheight] % rawdata_downsampled
        [ylabel=$S(f)$ (\unit{\pixel^2\per\hertz}), xlabel = $f$ (\unit{\per\hertz})] % PSD

    % \addplot[blue, thin] table[col sep=comma, x=t, y=x]{data_downsampled.csv}; % rawdata_downsampled

    % % \addplot+[fill=blue!40, hist={density, data=y, bins=50}, draw=none, no marks] table[col sep=comma, x=t,y=x] {data_downsampled.csv};
    % \addplot+[ybar,bar width=0.0759314, no marks, fill=blue!40] table[x=x,y=p, col sep=comma] {gaussian_data.csv};
    % % \addlegendentry{Empirical PDF}
    % \addplot[red, thick, domain=-1:1, samples=1000]{exp(-0.5*27.47123959*(x-0.04344942)^2+0.5*ln(27.47123959/(2*pi)))}; % See ipython file for curve fit code. 
    % % \addlegendentry{Fit to $\exp(-k(x-x_0)^2/(2\kB T_c)+\ln(k/(2\pi\kB T_c)))$}

    % brownian_PDF

    % \addplot[only marks, red] table[col sep=comma, x=x,y=E]{gaussian_data.csv};
    % \addplot+[only marks,error bars/.cd,y dir=both,y explicit] table[x=x,y=E,y error=dE,col sep=comma]{gaussian_data.csv};
    % \addplot[red, thick, domain=-1:1, samples=200] {0.5*27.47123959*(x-0.04344942)^2-0.5*ln(27.47123959/(2*pi))}; % See ipython for curve fit

    \addplot[blue!50, thin, no marks] table[col sep=comma, x=f, y=S]{PS_b2.txt};
    \addplot[olive, thick, no marks] table[col sep=comma, x=f, y=S]{theoretical_PSD.csv};
    \end{loglogaxis}
\end{tikzpicture}
\end{document}